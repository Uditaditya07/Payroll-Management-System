# Frontend (React + Tailwind)

Payroll Payment UI built with React and Tailwind CSS.

## Commands

From this directory:

```bash
npm install
npm run dev
npm run lint
npm run build
npm run preview
```

## Production delivery

- Build with `npm run build`.
- Serve/deploy only the generated `dist/` directory.
- Do not expose development source files in runtime deployments.
